{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --open --port 8989",
    "generate": "nuxt generate",
    "preview": "nuxt preview"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.1"
  }
}
